export const PASSWORD_MANAGER_CONFIG = {
	// Defines how many days before a password is considered "old" and a warning is shown
	PASSWORD_EXPIRY_WARNING_DAYS: 90,

	// Milliseconds in a day
	MS_PER_DAY: 24 * 60 * 60 * 1000,
};
